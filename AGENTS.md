# この作業ディレクトリの継続ルール

- カメラ設定は変更しない。露光、ゲイン、トリガ、ROI、PixelFormat、ReverseX/Y、ストリーム設定を含む。既存の設定を読み取り、外部ハードウェアの10 Hzトリガで取得する。
- 実カメラを使用するアプリを重複起動しない。動作確認はまず `--simulate` で行う。
- cam0（26259157）はユーザー設定で ReverseY が有効。保存画像は既に反転済みなので、位置合わせ・補正で再度反転しない。
- 今後の**すべての光伝搬計算**（Gabor再生、焦点探索、GS位相回復など）は、入力場を中央に配置した **4096 × 4096** 配列で行う。周囲は入力場の**平均値**で埋める。ゼロ埋め、サイズの自動変更、暗黙の縮小はしない。4096を超える入力は明示的なエラーとする。
- 振幅場を伝搬するときの平均値は振幅場の平均。Gaborでは `U0 = sqrt(I)` として `mean(U0)` で埋める。複素場は複素平均で埋める。計算後は中央の元の画像領域を切り出す。
- 共通実装は `260909/registration/optical_padding.py`。波長は515 nm、現カメラのピクセルピッチは2.74 µm。光伝搬とは無関係な画像補間・相関用FFTはこのパディング規則の対象外。
- 新しい角スペクトル法の計算は、伝搬距離ごとに2次元の伝達関数サンプリング条件を満たす帯域制限を適用する。各方向の `q_j = 2|z||f_j| / (L_j sqrt(lambda^-2-fx^2-fy^2))` を1未満に制限し、数値誤差の余裕を設け、帯域内の端5%をcosineで減衰させる。矩形の1次元近似だけで済ませない。共通実装は `260909/registration/optical_bandlimit.py`。GSの各往復伝搬も対象だが、全帯域が減衰開始点より内なら通過させる。これは撮影範囲外の縞の復元や周期境界誤差全般の保証ではない。
- 位置合わせはサブピクセルで推定し、補正マップはfloat座標を保持する。画質を優先し、画像変換の既定は **Lanczos4**。補正は元画像に一度だけ適用する。
- 撮影アプリは起動時Rec OFF。明示的なRec開始から停止までに受信した全画像を保存する。自動物体検出、自動保存、録画前後の自動追加保存は実装しない。
- Recボタンと、RでRec開始・停止、Nで表示コントラスト変更、Q/Escで終了する操作案内を画面に常時表示する。
- すべての再構成計算（Gabor再生、焦点探索、GS位相回復、MinIP生成を含む）はリモートで実行し、再構成画像・数値配列などの出力もリモートに保持する。ローカルでは再構成計算を実行しない。
- ローカルに新規保存してよいものは、生のホログラム、処理用スクリプト、軽量なHTML形式のレポートのみ（本AGENTS.mdなど作業ルールの文書更新は除く）。レポートは関連アセット・埋め込み画像をすべて含めて1件あたり **50 MB（50,000,000 bytes）以内** とし、転送前後に容量を確認する。レポート用の必要最小限の画像以外の再構成画像、配列、動画、アーカイブをローカルへ複製しない。
- 冗長な中間出力をできるだけ抑える。深さごとの再構成画像スタックや重複形式の保存を既定で行わず、MinIP等はメモリ内で逐次集約する。代表フレームと比較に必要な結果に絞り、全データの可視化・転送を目的にしない。
- 新しい実装はローカルの `260909` 以下、新しい解析結果はリモートの対応する `260909` 以下へ保存する。既存の結果は明示的な削除指示なしに削除せず、条件変更後の計算で古いキャッシュを再利用しない。
